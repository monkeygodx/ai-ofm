export default function TrustBar() {
  const logos = ['FluxMGMT', 'NovaOFM', 'CORE|DATA', 'RampFM', 'MetricOFM']

  return (
    <div className="trust">
      <div className="trust-l">Operators trained inside Vault</div>
      <div className="trust-logos">
        {logos.map((name) => (
          <div key={name} className="tl">{name}</div>
        ))}
      </div>
    </div>
  )
}
